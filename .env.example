# Mattermost Server Configuration
MATTERMOST_URL=https://mattermost.example.com

# Authentication - Choose one method:

# Method 1: Personal Access Token (Recommended)
MATTERMOST_TOKEN=your_personal_access_token_here

# Method 2: Username & Password (alternative)
# To use this method, edit docker-compose.yml to replace --token with:
# - --login
# - ${MATTERMOST_LOGIN}
# - --password
# - ${MATTERMOST_PASSWORD}
# MATTERMOST_LOGIN=user@example.com
# MATTERMOST_PASSWORD=your_password_here

# Optional: Custom port (uncomment if needed)
# MATTERMOST_PORT=8065

# Optional: Use HTTP instead of HTTPS (uncomment if needed, dev only)
# MATTERMOST_SCHEME=http
